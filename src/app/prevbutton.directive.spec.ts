import { PrevbuttonDirective } from './prevbutton.directive';

describe('PrevbuttonDirective', () => {
  it('should create an instance', () => {
    const directive = new PrevbuttonDirective();
    expect(directive).toBeTruthy();
  });
});
