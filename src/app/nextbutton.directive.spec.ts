import { NextbuttonDirective } from './nextbutton.directive';

describe('NextbuttonDirective', () => {
  it('should create an instance', () => {
    const directive = new NextbuttonDirective();
    expect(directive).toBeTruthy();
  });
});
